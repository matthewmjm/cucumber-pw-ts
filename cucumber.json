{
	"default": {
		"dryRun": true,
		"formatOptions": {
			"snippetInterface": "async-await"
		}
	},
	"paths": ["e2etests/tests/features/**/*.feature"],
	"require": ["e2etests/tests/steps/**/*.ts"],
	"requireModule": ["ts-node/register"],
	"format": []
}
